import React, { Component } from 'react';

export default class Inventory extends Component {
    render() {
        return (
            <div class='inventory  grow'>Inventory and Money</div>
        )
    }
}